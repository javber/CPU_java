package tp.pr5.mv.modulos.controladores;

import tp.pr5.mv.modulos.CPU;

// Este controlador realmente no es necesario, solo se mantiene
// por flexibilidad
public class ControladorConsola extends Controlador{	
	
	public ControladorConsola(CPU cpu) {
		super(cpu);
	}
}
